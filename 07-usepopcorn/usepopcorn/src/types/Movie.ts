/**
 * Movie interface
 * @interface Movie
 * @property {string} imdbID - The IMDb ID of the movie.
 * @property {string} Title - The title of the movie.
 * @property {string} Year - The year of the movie.
 * @property {string} Poster - The poster URL of the movie.
 */
export interface Movie {
  imdbID: string;
  Title: string;
  Year: string;
  Poster: string;
}
